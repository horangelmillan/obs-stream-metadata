# FINDINGS — hallazgos

Formato: `F-### | fecha | contexto | descubrimiento | evidencia/fuente | impacto | decisión | tareas`.

| ID | Fecha | Contexto | Descubrimiento | Evidencia / Fuente | Impacto | Decisión | Tareas |
|---|---|---|---|---|---|---|---|
| F-001 | 2026-09-07 | Capacidades MVP | Título ≠ descripción: Twitch/Kick sin descripción de stream equivalente; solo YouTube la soporta | `AGENTS.md` §2-3, §47 | UI no debe prometer "aplicar descripción a todo" | Modelo por plataforma (matriz §3) | T-010, T-030 |
| F-002 | 2026-09-08 | Entorno Windows | Sin cmake/VS/Qt SDK en PATH; OBS 32.2.2 con Qt6 DLLs instalado | `obs64.exe 32.2.2`, `bin\64bit\Qt6*.dll` | No compilar en Phase 0; toolchain pendiente | Registrar deuda T-011, no instalar sin ADR | T-011 |
| F-003 | 2026-09-07 | Kick API | `PATCH /public/v1/channels` con `stream_title` → `204`; `channel_description` ≠ descripción de stream | `AGENTS.md` §14.5-14.6 | No escribir `channel_description` como descripción | Título Kick sí; descripción no soportada | T-030 |
| F-004 | 2026-09-07 | YouTube | Hay que seleccionar `liveBroadcast` antes de actualizar; no crear broadcasts en MVP | `AGENTS.md` §21, §41, §48 | Flujo account→broadcast→snippet | Selector de broadcast en UI futura | T-030 |
| F-005 | 2026-09-08 | CI secret-scan | Patrón ingenuo (palabras sueltas) marcó docs legítimos; apóstrofe en patrón rompió bash (exit 2) | runs PR #1 `34187529299`, `34187597990` | CI rojo en 2 intentos | Escanear valores (asignaciones/prefijos/PEM), sin apóstrofes en `run:` | T-001 |
| F-006 | 2026-09-08 | Toolchain Windows | OBS 32.2.2 local lleva Qt **6.11.1** (`Qt6Core.dll`); template fija obs-studio **31.1.1** en `buildspec.json` (hay que subir a 32.2.2 en P1); toolchain (VS/CMake/Qt SDK) ausente en PATH | `C:\Program Files\obs-studio\bin\64bit\Qt6Core.dll` 6.11.1.0; `obs-plugintemplate` `CMakeLists.txt` (`3.28...3.30`) + wiki Build System Requirements (VS17 2022, SDK 10.0.22621, CMake 3.30.5) | Qt no se instala manual (lo da obs-deps); R1-R4 en ADR-005 | ADR-005; instalar en T-012 | T-012, T-020 |
| F-007 | 2026-09-08 | master vs main | Ninguna razón técnica/CI/GitHub/tooling obliga a `main`: `default_branch: master`, sin branch protection, CI anclado a `master`, template oficial también usa `master` | `gh api repos/...` (`default_branch: master`, 404 en protection); `ci-phase0.yml` `branches: [master]`; árbol `i/lf w/lf` limpio | Se mantiene `master`; `.gitattributes` `* text=auto` independiza LF del `autocrlf` local | ADR-004 | — |
| F-008 | 2026-09-08 | Toolchain T-012 + wiki OBS-master | Toolchain instalado y validado (VS2022 17.14.37614.0, MSVC 19.44.35228, ATL, SDK 10.0.22621.0, CMake 3.31.6 bundled, Git 2.49.0; OBS 32.2.2 + Qt 6.11.1; sin Qt manual). Discrepancia nueva: wiki `obs-studio` Build Instructions (ed. 2026-06-20) pide VS2026 + SDK 26100 + CMake 4.2 — es para compilar OBS-master, NO el template (wiki template 2024-11-01 sigue en VS2022/SDK22621/CMake3.30.5 y `CMakeLists.txt` master exige `3.28...3.30`). Cierra F-002 (toolchain). | `vswhere` + `cl.exe` + `atlbase.h` + `Windows Kits\10\Include\10.0.22621.0` locales; `cmake --version` 3.31.6; configure de prueba con rango `3.28...3.30` ok; wikis oficiales consultadas 2026-09-08 | ADR-005 sigue válido para el template; P1 debe comprobar requisitos del tag 32.2.2 (no de master) al subir `buildspec.json`; CMake puede auto-seleccionar SDK 26100 — fijar `CMAKE_SYSTEM_VERSION=10.0.22621.0` si P1 lo requiere | T-012, P1 |
| F-009 | 2026-09-08 | Lanzar OBS 32.2.2 en Windows para validar plugin | OBS **debe** lanzarse con CWD = `bin\64bit` (o que resuelva `data/obs-studio/`): `GetDataFilePath` prueba primero ruta relativa a CWD y luego `OBS_DATA_PATH`; con CWD incorrecto el arranque aborta con `Failed to find locale/en-US.ini` (fatal, antes de cargar ningún plugin). | `frontend/utility/platform-windows.cpp:51-58` (`check_path(data,"data/obs-studio/")` → `OBS_DATA_PATH`) + `frontend/OBSApp.cpp:732-736` (throw) del tag 32.2.2; capturas de diálogo + 0 logs hasta corregir CWD | Procedimiento P1: `Start-Process obs64.exe -WorkingDirectory '<install>\bin\64bit'`; documentado en TROUBLESHOOTING | T-013 |
| F-010 | 2026-09-08 | Dónde poner el plugin sin admin en Windows | En Windows la ruta per-usuario escaneada es **ProgramData** (`GetProgramDataPath "obs-studio/plugins/%module%"`), NO `%APPDATA%` (esa solo vale en macOS/Linux). `%APPDATA%\obs-studio\plugins\...` se ignora en silencio. Para validación sin admin: variables `OBS_PLUGINS_PATH` (dir con el `.dll`) + `OBS_PLUGINS_DATA_PATH` (base de `data/<módulo>`). | `frontend/widgets/OBSBasic.cpp:143-184` (`AddExtraModulePaths`, tag 32.2.2); 2 arranques con env-vars → módulo en memoria + log `[obs-stream-metadata] plugin loaded successfully (version 0.1.0)` | Instalación dev vía env-vars (TROUBLESHOOTING); instalador final (P7) usará `obs-plugins/64bit` + `data/obs-plugins` | T-013 |
| F-011 | 2026-09-08 | Safe-mode tras matar OBS en tests | Cada arranque crea `.sentinel/run_<uuid>` en `%APPDATA%\obs-studio`; al matar el proceso quedan restos y el siguiente arranque muestra el diálogo safe-mode (bloquea validación automática). Cierre elegante (`CloseMainWindow`) los limpia. Borrar `run_*` obsoletos es seguro (solo marcan shutdown no limpio). | `frontend/utility/CrashHandler.cpp:36,186-227,353-391` (tag 32.2.2); 6 sentinels obsoletos tras `Stop-Process`; diálogo capturado; tras limpiar + cierre elegante: sentinel vacío, sin crashes nuevos | Tests P1+: siempre cierre elegante; jamás `kill` salvo que se limpie sentinel después | T-013 |
| F-012 | 2026-09-08 | R2 cerrado: Qt exacto + unload/reload | Qt del obs-deps 2026-07-15 es **6.11.1.0**, idéntico al Qt embebido de OBS 32.2.2: cero skew. Unload/reload en caliente NO existe para módulos nativos en OBS 32.2.2 (sin UI/CLI/API de usuario; `obs_module_unload` solo corre al cerrar). Ciclo validado: shutdown→`plugin unloaded` en log, rearranque→carga de nuevo, sin crash ni sentinels. Warning benigno `Failed to load 'en-US' text` afecta también a módulos bundled (audio-monitor, decklink): no bloquea P1. | `Qt6Core.dll` 6.11.1.0 en `.deps/obs-deps-qt6-2026-07-15-x64` y en `C:\Program Files\obs-studio\bin\64bit`; logs `2026-09-08 09-21-06.txt` + `09-22-57.txt` (load+unload ×2); `plugin_manager/modules.json` | P2 puede asumir Qt 6.11.1; unload/reload en caliente queda documentado como limitación de OBS, no del plugin | T-013, T-020 |
| F-013 | 2026-09-08 | Dock nativo OBS 32.2.2 (T-020) | `obs_frontend_add_dock_by_id(id,title,QWidget*)` es el mecanismo correcto para la PoC: OBS crea el `OBSDock` (dueño: main window), reparenta el widget (`setWidget`), añade toggle al menú Paneles, arranca flotante+oculto. `add_custom_qdock` solo si el plugin necesita su propio QDockWidget (no es el caso). Ciclo seguro: módulos cargan dentro de `OBSBasic::Init` (API válida en `obs_module_load`); en shutdown los callbacks se anulan ANTES de `obs_shutdown()` → `obs_frontend_remove_dock` en unload es no-op seguro; el plugin JAMÁS debe borrar el widget (OBS lo destruye; tras remove el puntero cuelga). ID estable `obs-stream-metadata-dock` → geometría/visibilidad restauradas por `saveState/restoreState` (verificado: posición 40,40 400x300 restaurada sin tocar nada). `dock->show()` en load pierde contra `restoreState` posterior: la visibilidad inicial real la decide el layout guardado; el toggle del menú Paneles es la vía del usuario. | `frontend/OBSStudioAPI.cpp:336-379`, `frontend/widgets/OBSBasic.cpp:1041-1056` (carga en Init), `:2042-2048` (callbacks=null antes de shutdown), `frontend/widgets/OBSBasic_Docks.cpp:143-196`, `frontend/utility/platform-windows.cpp` no aplica; logs `11-04-48/11-08-39/11-10-04/11-11-59.txt` (`dock created (3 labels)`, `dock shown`, `dock removed`, `plugin unloaded` ×5 ciclos); HWND `OBSDock` "Stream Metadata" + restauración de geometría | Base para el dock unificado P4 (mismo id, mismo ownership, misma política show); P4 refinará visibilidad de primer arranque | T-020 |
| F-014 | 2026-09-08 | Límites de validación UI sin sesión interactiva | En esta sesión (sin foreground): `SetForegroundWindow`/`SendKeys` no funcionan; menús Qt no se abren por teclado automatizado; `PrintWindow` NO captura contenido de widgets Qt (verificado con dock propio y con dock "Estadísticas" de OBS: marco sí, contenido no); UIA solo expone el chrome OS, no el contenido Qt; `CopyFromScreen` captura otro desktop. Verificado por otras vías: registro (log + HWND `OBSDock`), contenido (autochequeo `findChildren<QLabel*>` → 3 labels en log), visibilidad/mover/cerrar (HWND VISIBLE + `SetWindowPos` + `WM_CLOSE`), persistencia (geometría restaurada), ciclo de vida (5 arranques limpios). Pendiente manual (30 s, con ratón): toggle en menú Paneles → mover/acoplar/desacoplar por arrastre y confirmar contenido visual. | Capturas `obs-main/menu/menu2/dock2/dock4/stats-ref.png`; enumeración UIA/HWND; 5 logs 11:00–11:12 | Ninguna decisión de arquitectura; registrar como límite del harness, no del producto | T-020 |

| F-015 | 2026-09-08 | Twitch OAuth en desktop (T-030) | El Authorization Code Grant exige secret en servidor; en un plugin de escritorio el secret embebido no es custodiable. La doc oficial ofrece **Device Code Flow** para clientes públicos en plataformas abiertas (Windows): sin secret, refresh de un solo uso, expira a 30 días inactivo, access ~4 h. PoC adopta device flow; `/validate` obligatorio tras cada sesión. | `dev.twitch.tv/docs/authentication/getting-tokens-oauth` (2026-09-08) + `docs/T030-POC.md` §2-3 | Sin secret de Twitch en el binario; PoC sin callback local para Twitch | T-030 |
| F-016 | 2026-09-08 | YouTube `liveBroadcasts.update` = PUT de recurso completo (T-030) | `update` **sobrescribe** las propiedades mutables de cada `part` incluida: si se envía `part=status` sin `privacyStatus`, el broadcast revierte a privacidad por defecto. El request debe llevar `id` + `snippet.scheduledStartTime` + `contentDetails.monitorStream.*`. Por eso la PoC exige GET previo y fusión (`youTubeSnippetPayload` preserva esos campos); `part=snippet` solo. Límites: título 1–100, descripción ≤5000. | `developers.google.com/youtube/v3/live/docs/liveBroadcasts/update` (ed. 2026-08-18, cons. 2026-09-08) + selfcheck `youtube-merge` | Selector de broadcast obligatorio en P4; jamás PUT solo con título/descripción | T-030, T-031 |
| F-017 | 2026-09-08 | Kick OAuth 2.1: secret obligatorio + localhost (T-030) | El token endpoint de Kick exige secret **incluso con PKCE** → un plugin desktop no puede custodiar un secret compartido (riesgo abierto: app registrada por el usuario o intermediario; no embeber secret). La doc exige `localhost`, no `127.0.0.1` (bug NextJS que reescribe el primer `127.0.0.1` y rompe el match exacto de redirect URI). | `KickDevDocs/.../generating-tokens-oauth2-flow.md` (2026-09-08) + `docs/T030-POC.md` §2-3 | Callback Kick = `http://localhost/...`; distribución del secret queda para P5 | T-030, T-032 |
| F-018 | 2026-09-08 | Prueba viva Twitch: el flujo manual con curl expone secretos en transcripts (T-030) | Durante la verificación viva el operador pegó en el chat `device_code`, `access_token` y `refresh_token` reales (transcript fuera del repo; tokens revocados tras la prueba, sin persistencia en Git). El flujo manual es propenso a fugas: el operador copia JSON completos. Mitigación: `tools/t030_live_twitch.py` (stdlib, device flow con salida redactada —tokens solo en memoria, nunca impresos— y revoke al final). Uso obligatorio para repeticiones; mismo patrón a aplicar en YouTube/Kick antes de sus pruebas vivas. | Transcript de sesión 2026-09-08 (revocado) + `tools/t030_live_twitch.py` (dry-run con ID falso → FAIL 400 limpio, exit 11) | Prohibido verificar con curl manual pegando JSON; usar runners redactados | T-030 |
| F-019 | 2026-09-08 | YouTube: Google exige `client_secret` incluso en clientes Desktop (T-030) | El token exchange sin secret falla con `400 invalid_request: client_secret is missing` aunque el cliente sea tipo Desktop + PKCE. Comportamiento Google conocido desde ~2025 (el secret Desktop se trata como extensión no-confidencial del ID). El runner pide el secret solo local (`--client-secret` / env `GOOGLE_CLIENT_SECRET` / pregunta oculta), nunca lo imprime ni commitea. | `docs/T030-POC.md` §2 ("Sin secret") queda matizado; foros Google Developer 2025-2026 + error vivo `400` de `oauth2.googleapis.com/token` | Corregir T030-POC §2 y P4: el plugin pedirá secret Desktop local o lo tratará como dato no-confidencial | T-030, T-032 |
| F-020 | 2026-09-08 | YouTube `liveBroadcasts.list`: filtros excluyentes (T-030) | La doc exige **exactamente un** filtro (`broadcastStatus` \| `id` \| `mine`): `mine=true` + `broadcastStatus=active` devuelve `400 Incompatible parameters specified in the request: mine, broadcastStatus`. El runner lista con `mine=true` solo y filtra por `lifeCycleStatus` en local. | `developers.google.com/youtube/v3/live/docs/liveBroadcasts/list` (ed. 2025-08-28, cons. 2026-09-08) + error vivo | Selector de broadcast P4: listar `mine` y filtrar en cliente | T-030, T-031 |
| F-021 | 2026-09-08 | YouTube `liveBroadcasts.update`: con `part=snippet` el body NO admite `contentDetails` (corrige F-016, T-030) | Enviar `contentDetails` con `part=snippet` falla con `400 unexpectedPart`. Basta `{"id","snippet"}` con el snippet completo (preserva `scheduledStartTime`, `categoryId`...). F-016 queda corregido: no hay que enviar `contentDetails.monitorStream`. Lógica offline (`youTubeSnippetPayload` + selfcheck `youtube-merge`) actualizada. | Error vivo `400 youtube.part unexpectedPart` + reintento con solo `id+snippet` → `PUT 200` + read-back OK | Actualizar T030-POC §2/F-016; P4 usa payload mínimo `id+snippet` | T-030, T-031 |
| F-022 | 2026-09-08 | Kick tras Cloudflare: urllib bloqueado con 403 code 1010 (T-030) | `POST id.kick.com/oauth/token` desde `Python-urllib` es rechazado por el Browser Integrity Check de Cloudflare (`403 error code: 1010`), no por la API de Kick. El runner envía cabeceras de navegador real (solo PoC local) y el intercambio pasa. | Error vivo `403 code: 1010` + fix con `User-Agent/Accept` → token OK | El cliente HTTP del plugin (P4/P5) debe usar UA de navegador; documentar en T030-POC | T-030, T-031 |
| F-023 | 2026-09-08 | Kick: PATCH 204 en offline persiste pero el título solo es legible en directo (T-030) | Con el canal offline, `PATCH stream_title` devuelve `204` pero `GET channels` sigue devolviendo `stream_title=''` (4 intentos en 60 s). Con el canal en directo, el mismo flujo devuelve el título en `GET channels` y en `GET users/livestreams`. Dato: el título del PATCH offline apareció como "original" en la prueba en directo posterior → el 204 offline sí aplica en servidor, pero no es verificable hasta el directo. Regla MVP: verificar Kick en directo; read-back por doble vía (channels + users/livestreams). | Dos sesiones vivas 2026-09-08 (canal `sonokigame`, `broadcaster_user_id=128456005`): offline `204` + read-back vacío; en directo `204` + read-back coincidente en ambas vías + revoke 200/200 | Read-back Kick exige directo activo; P6 debe probar ambos estados | T-030, T-033 |
| F-024 | 2026-09-08 | Códigos OAuth en capturas de pantalla (extiende F-018, T-030) | Durante la prueba de YouTube el operador compartió una captura con el `authorization code` visible en la barra de URL. Riesgo bajo (protegido por PKCE —inútil sin el verifier local—, un solo uso, caduca en minutos; no se emitió ningún token porque el intercambio falló), pero prohíbe el patrón: no compartir barras de URL con codes. | Captura de sesión 2026-09-08 (code expirado/consumido, sin tokens emitidos) | Ni codes ni tokens en chat/capturas; runners nunca los imprimen | T-030 |
| F-025 | 2026-09-08 | Dock MVP P4 (T-031) | Qt6 Network viene en obs-deps y basta `QNetworkAccessManager` por señales (sin hilos ni libs async) para OAuth/HTTP sin congelar la UI; el gate CI de P1 (prohibir palabras de proveedor en `src/`) debe evolucionar a gate P4 (código de producto en `src/`, PoC intacta, scan de valores) o todo PR P4 falla CI en verde local. | Build 0 errores + `dock created (metadata mvp)` en OBS 32.2.2 ×2 ciclos; `.github/workflows/ci-phase0.yml` (gate P4) | Async con QNAM + estado pendiente-en-reply; gate exige `src/metadata.h`/`metadata_dock.h` | T-031 |
| F-026 | 2026-09-08 | Defectos reales T-031 pre-live (revisión contra runners P3) | (1) El callback pedía puerto efímero también para Kick, pero Kick exige el redirect EXACTO registrado → el intercambio habría fallado siempre; fix: puerto fijo 3000 con error claro si está ocupado (YouTube/loopback sí admite efímero). (2) El servidor de callback procesaba TODA request (incl. `/favicon.ico` del navegador) → `state mismatch` espurio que rompía el Connect; fix: solo la primera request a la ruta exacta de callback gana, el resto recibe 200 sin efecto. | `src/metadata_dock.cpp` (`listenCallback`, `handleCallbackData`, `onConnectKick`); build 0 errores + selfchecks 19/19+28/28 + ciclo OBS limpio post-fix | First-callback-wins + puerto documentado = registrado | T-031 |
| F-027 | 2026-09-08 | HTTPS imposible desde el dock + DATA_PATH dev off-by-one (T-031 live T1) | (1) OBS no incluye el plugin TLS de su propio Qt (`bin/64bit` sin carpeta `tls/`; Qt 6.11 usa backend Schannel por plugin) → todo HTTPS de `QNetworkAccessManager` fallaba con "Could not reach". Fix: empaquetar `qschannelbackend.dll` (release, del obs-deps pineado) en `data/tls/` vía CMake + `addLibraryPath(obs_get_module_data_path)` + log `tls backend ready`. (2) El mapeo dev de `OBS_PLUGINS_DATA_PATH` estaba un nivel mal: OBS antepone `%module%` (`OBSBasic.cpp:136-139`), así que el data del módulo debe vivir en `DATA_PATH/obs-stream-metadata/`; con el layout del template hay que copiar `data/*` a `devdata/obs-stream-metadata/` y apuntar DATA_PATH a `devdata`. (3) Los errores de Connect no distinguían red de 400 → mensaje engañoso; fix: `connectHttpError` (red / 400-config / resto) + log con código HTTP redactado. | `src/dock-poc.cpp`, `src/metadata_dock.*`, `CMakeLists.txt`; evidencia OS: `Invoke-WebRequest` a `id.twitch.tv` → 400 (red OS ok) | Nunca asumir TLS en el Qt de OBS; verificar `tls backend ready: yes` en log | T-031 |
| F-028 | 2026-09-08 | Dock acoplado deforma OBS + resultado no refleja conexión (T-031 live) | El formulario sin scroll forzaba el ancho del área acoplada y deformaba la ventana principal. Fix: cuerpo en `QScrollArea` con `widgetResizable` (el panel hace scroll en vez de ensanchar). Además la fila de resultado ahora muestra `connected` al conectar (antes quedaba en `—` y confundía). | Captura del operador + `src/metadata_dock.cpp` | Todo dock acoplable va con scroll | T-031 |

| F-029 | 2026-09-09 | Storage seguro Windows (T-032) | DPAPI `CurrentUser` + `CRYPTPROTECT_UI_FORBIDDEN` basta sin dependencias nuevas (solo `crypt32` del SDK): blob base64 por plataforma en `accounts.json` bajo `obs_module_config_path` (`%APPDATA%\obs-studio\plugin_config\obs-stream-metadata\`); etiquetas (display/broadcaster) en claro, secretos jamás. `QSaveFile` para escritura atómica. Fuera de Windows se rehúsa persistir (fallback memoria). | `src/secure_store.*`; selfcheck `store-*` (round-trip + no-plaintext + clear) | Cierra el punto 1 de P5; MVP declara Windows como objetivo | T-032 |
| F-030 | 2026-09-09 | Revoke real en Disconnect (T-032) | Mismo wire format que los runners P3 (Twitch: `POST revoke?client_id&token` cuerpo vacío; YouTube: form `token`; Kick: form `token` + UA navegador). Local se borra primero (UI inmediata), revoke en segundo plano encadenando access+refresh, solo códigos en log, best-effort. | `src/metadata_dock.cpp` (`startRevoke/sendNextRevoke`, `Op::Rev*`); `meta::revokeEndpoint` + selfcheck `revoke-*` | Cierra el punto 2 de P5 | T-032 |
| F-031 | 2026-09-09 | Backoff acotado 429/5xx (T-032) | Máx 2 reenvíos por plataforma (2 s, 4 s) preservando el presupuesto de refresh-único (`backoffResume_`): ni loops 429 ni refresh múltiples. La lista de broadcasts informa sin reintentar (acción del usuario). Constantes puras en `meta::` con selfcheck `backoff-*`. | `src/metadata.*`, `src/metadata_dock.cpp` (`scheduleBackoff/onBackoffTimeout`) | Cierra el punto 3 de P5 | T-032 |
| F-032 | 2026-09-09 | Fin de env vars + BYO-app (T-032) | 5 campos en el dock (IDs en claro, secrets con echo Password) sustituyen a `STREAM_META_*` (código `env()` eliminado). Decisión ADR-008: el operador registra 1 app por plataforma; secret compartido embebido prohibido; DPAPI custodia lo escrito. | `docs/DECISIONS/ADR-008-byod-secrets-dpapi.md`; `src/metadata_dock.*` | Cierra los puntos 4-5 de P5; `TROUBLESHOOTING.md` T-032 documenta el alta | T-032 |
| F-033 | 2026-09-09 | Auditoría OAuth: Twitch public-client | Device Code Flow admite public clients sin Client Secret; Client ID es identificador distribuible; T-030 reutilizable sin cambiar UX (`Connect → Device Flow → Connected as`). Distribución concreta del Client ID queda como detalle pendiente, no BYO-app. | `dev.twitch.tv/docs/authentication/getting-tokens-oauth` (Device Code Grant Flow, public vs confidential, Windows→public); `dev.twitch.tv/docs/authentication/register-app` (Client ID público embebible; secret nunca exponer) | Confirma vía directa para UX objetivo | T-030, decisión final pendiente |
| F-034 | 2026-09-09 | Auditoría OAuth: YouTube secretless documentado, no validado | Doc oficial Native/Installed Apps lista `client_secret` como `Optional` en code-exchange y refresh, con sample sin secret; installed apps `cannot keep secrets`; PKCE + loopback para desktop. Pero T-030 y reportes externos 2025–2026 muestran `400 invalid_request: client_secret is missing` en ciertas configs Desktop. Conclusión: oficialmente documentado, empíricamente pendiente para nuestra configuración (`youtube.force-ssl` + Desktop + PKCE + loopback). | `developers.google.com/identity/protocols/oauth2/native-app` (Step 5/refresh `client_secret Optional` + sample); `developers.google.com/youtube/v3/guides/auth/installed-apps`; `google-api-python-client/docs/oauth-installed.md`; T-030 F-019 + foros Google 2025–2026 + PoC Electron 2025-07 (terceras partes) | No afirmar "siempre funciona" ni "siempre requiere"; requiere prueba empírica aislada (NO realizar en tarea documental) | Prueba YouTube secretless pendiente |
| F-035 | 2026-09-09 | Auditoría OAuth: YouTube Client ID independiente | `Secretless ≠ distribución resuelta`: aun sin secret, la política Google/open-source sobre Client ID (ToS 4b, `client_secrets.json` fuera del source tree) sigue pendiente. | `google-auth-library-nodejs#959` (tercera parte: ToS 4b); `google-api-python-client/docs/oauth-installed.md` | Estrategia release privada/artefacto o backend por estudiar | Investigación distribución Client ID Google pendiente |
| F-036 | 2026-09-09 | Auditoría OAuth: Kick exige secret, sin Public Client oficial | Token authorization-code y refresh exigen `client_secret`: Required oficialmente; no se encontró `public client` / `client authentication=none` / secret opcional / credential desktop. PKCE es adicional, no sustituto. Wrappers terceros que sugieren omitir secret son **terceras partes**, no evidencia oficial. Revisados docs oficiales, app setup, swagger/API, KickDevDocs e issues (#185, #383). | `docs.kick.com/getting-started/generating-tokens-oauth2-flow.md`; `KickDevDocs/.../generating-tokens-oauth2-flow.md`; `api.kick.com/swagger`; `kick-apps-setup.md` | Arquitectura más probable: `Connect → browser → backend/intermediario → tokens → Connected as`; plugin nunca recibe/guarda Kick secret; queda prueba secretless aislada opcional (NO realizar ahora) | Prueba Kick secretless opcional; backend Kick pendiente de decisión |
| F-037 | 2026-09-09 | Auditoría: BYO-app como etapa, no UX final | BYO-app/T-032 fue solución válida de etapa operador/desarrollo (custodia DPAPI, revoke, backoff, fin env vars); la auditoría determina que no coincide con UX objetivo (`Connect` sin crear apps ni pegar secrets). Conservar historia, no marcar T-032 "incorrecto" retrospectivamente. | Auditoría 2026-09-09 + ADR-008 (nota estado) + `AGENTS.md` §36-38 | Migración UX pendiente; ADR-009 posterior tras pruebas/decisión | Decisión arquitectura final + ADR-009 pendientes |
| F-038 | 2026-09-09 | T-035 FAIL: YouTube secretless refutado | Exchange real (Desktop + PKCE S256 + loopback + `youtube.force-ssl` + code real, sin secret) → `400 invalid_request: client_secret is missing`. Autorización y callback válidos; el rechazo es específico del secret. | Experimento aislado T-035 (script temporal externo, eliminado; sin cambios a repo) | Cierra F-034 como FAIL; YouTube requiere secret en esta configuración; remite a distribución (T-037) | T-037, T-038/ADR-009 |
| F-039 | 2026-09-09 | T-036 FAIL: Kick secretless refutado | Secretless (auth + PKCE + `localhost:3000/cb`, UA navegador) → `400` vacío; control idéntico con secret → `200` con access+refresh (descartados). Causa aislada: ausencia de secret. | Experimento aislado T-036 + control (scripts temporales externos, eliminados) + `docs.kick.com` (secret Required) | Kick requiere secret; backend necesario para UX objetivo | T-038/ADR-009 |
| F-040 | 2026-09-09 | T-037: sin permiso oficial para secreto en binario público | ToS (credenciales confidenciales, prohibidas en open-source) + Policies (extreme care, secret manager, jamás repo público) + best practices (no hardcode/commit/publish) vs installed-apps (“cannot keep secrets”, embed contemplado pero `client_secrets.json` fuera del árbol GitHub). Release-privada resuelve repo, no extracción/quota/suplantación. Backend, vía conforme. | `developers.google.com/terms`, `/identity/protocols/oauth2/policies` (2026-08-05), `/resources/best-practices` (2026-05-20), `native-app`, `youtube/.../installed-apps`, `oauth-installed.md` | Descarta embebido/release-privada como solución conforme; fundamenta backend YT | T-038/ADR-009 |
| F-041 | 2026-09-09 | T-038 MODIFY: backend YouTube+Kick, Twitch directo | Backend centralizado Hexagonal/Shared Kernel aceptado con alcance modificado: obligatorio donde hay secreto (YT/Kick), Twitch directo (DCF público, sin secreto que custodiar). Plugin sin secrets ni refresh YT/Kick; auth sin secreto permanente (instalación + sesiones cortas + límites). | T-035/T-036/T-037 + matriz §18 ADR-009 | Implementación T-043+; ADR-008 pasa a modo operador/debug | T-043–T-048 |
| F-042 | 2026-09-09 | T-043 PASS: fundación backend stdlib | `backend/` sin dependencias (Python stdlib, coherente con `tools/`): kernel/ports (`OAuthProvider`, `TokenStore`, `SessionStore`, `SecretStore`, `RateLimiter`), adapters YouTube/Kick/Twitch como estructura sin red, `GET /health|/ready|/version`, error model §28 sin fugas, stores dev-only marcados, 27 tests en verde + smoke real. `dev.env` (material del operador) añadido a `.gitignore`. Sin cambios en `src/`. | Tests + smoke 2026-09-09; ADR-009 | Base para T-044 (auth) sin reescribir | T-044+ |
| F-043 | 2026-09-09 | T-044 PASS: auth sin secreto global (keypair descartado) | Bootstrap anónimo rate-limitado → secreto por instalación (DPAPI) → sesión opaca 30 min (memoria) → refresh HMAC+nonce un solo uso → revoke sesión/instalación. Keypair asimétrico descartado: mismas propiedades bajo compromiso de endpoint, HMAC-SHA256 stdlib ambos lados. Un test detectó que `validate_session` ignoraba revocación de instalación (corregido). Cliente Qt async `src/backend_auth.*` (sin UI; wiring T-048); `secure_store` extendido con `backendInstall` backward-compatible. 43 tests backend + selfcheck 68/68 + build/DLL 0 errores. | ADR-010; tests + build 2026-09-09 | T-045/T-046 usan sesiones sin redefinirlas | T-045+ |
| F-044 | 2026-09-09 | T-045 PASS: YouTube vía backend en vivo | `POST /connect/youtube` → browser → callback backend → exchange con secret (solo backend) → identidad `channels.mine` → `Connected as` → disconnect+revoke. Replay/state/expiración rechazados (tests). Loopback Desktop aceptó path `/connect/youtube/callback` en 127.0.0.1:9004 (Observado; contrasta con Kick F-017). Refresh single-flight + `invalid_grant`→re-conectar. 57 tests + live PASS + secret-scan limpio. Producción (verificación, HTTPS, secret manager) pendiente. | ADR-011; live 2026-09-09 (canal 10 chars, valor no registrado) | Patrón para T-046; contrato listo para T-048 | T-046, T-048 |
| F-045 | 2026-09-09 | T-046 PASS: Kick vía backend en vivo | `POST /connect/kick` → browser → callback `localhost:3000/connect/kick/callback` → exchange con secret (solo backend) → identidad `channels` (slug + broadcaster_user_id) → `Connected as` → disconnect+revoke → `disconnected`. Observado: con redirect no registrada, el navegador aterriza en `api.kick.com/oauth/authorize` con `invalid authorization request` (Inference: validación en ese host); tras registrar la URI exacta, PASS. Confirma match exacto incl. path (F-017) y que el error era de authorization, no de secret. `ConnectService` generalizado a multi-provider sin contaminar kernel (fetch_identity al port). 69 tests + live PASS + secret-scan limpio. Sin ADR nuevo (mismo patrón T-045). | Live 2026-09-09 (slug no registrado); docs.kick.com (sin cambios vs T-036) | T-048 cablea Connect/status; producción pendiente | T-048 |
| F-046 | 2026-09-09 | Auditoría dos-modalidades: BYO-app reenmarcado | `BYO-app no es UX final` (STATE) solo era válido bajo UX única sin credenciales. Con ADR-012, BYO-app = UX del modo Independiente; el dock actual (5 campos universales + OAuth directo) es su embrión válido. Cambia a configuración contextual (Twitch: solo ID). `STREAM_META_*` siguen sin resucitar. | `obs-stream-metadata-arquitectura-dos-modalidades.md` §13 + `src/metadata_dock.cpp:120-133` (campos) | T-041/T-049; ADR-008 intacto | T-049+ |
| F-047 | 2026-09-09 | Auditoría: backend = base Managed, no obligatorio | ADR-009 como “arquitectura de distribución” queda acotado a modalidad Administrada. `backend/` + `backend_auth.*` (sin cablear en el dock: 0 referencias desde `metadata_dock`) = base Managed reutilizable sin rewrite. Independiente no necesita backend. | ADR-012; grep `BackendAuth` en `src/` | T-048/T-051; sin cambios de código en esta tarea | T-048+ |
| F-048 | 2026-09-09 | Auditoría: asimetría redirect YT vs Kick | Loopback Google aceptó path nuevo sin registro previo (Observado T-045); Kick exige match exacto incl. path (Observado T-046). Implicación: alta de redirect es fricción real del modo Independiente en Kick; documentarla en la guía de configuración. | Lives T-045/T-046 | Guía Independiente (T-049) | T-049 |
| F-049 | 2026-09-09 | Auditoría integral: alineación dos-modalidades | Revisados `src/` (14 ficheros), `backend/` (26 `.py`), docs y T-030–T-046: sin contradicciones residuales tras ADR-012 (BYO-app→Independiente, backend→Managed, T-041/T-048 reenmarcadas, T-042 SUPERSEDED por T-046, P8 creada). Reutilización §7 verificada pieza por pieza en código. `AGENTS.md` se conserva intacto como investigación histórica; STATE es la verdad vigente. VALIDATION/PHASES/TROUBLESHOOTING alineados (matrices por modalidad pendientes T-050–T-052). | Esta auditoría | T-049+ implementa | T-049+ |
| F-050 | 2026-09-09 | T-049 PASS: `ConnectionMode` mínimo sin duplicar | Sin concepto previo (ni dock, ni store, ni backend). Enum en `metadata.h` (dominio común) + `connectionModeName`/`defaultConnectionMode`/`parseConnectionMode` + 7 checks; `metadata.*`/Apply intactos; backend sin representación (su `Connection` ⟺ contexto Managed; Independiente jamás lo toca); sin persistencia (opción D documentada: sin consumidores aún); sin UI/wiring. 75/75 + 69/69 + build/scan en verde. | T-049; sin ADR nuevo (ADR-012 ya decide) | T-041 persiste y consume | T-041 |

Los hallazgos importantes se consolidan en `AGENTS.md`/docs permanentes cuando corresponda.
